package LinkedList;
//Given a linked list, split it into lists such that one contains odd values, while the other contains even values.
public class splitLL {
    public static void main(String[] args) {

    }
}
